<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Kibitz Fenster', root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Objave', articles_path  %></li>
        <li><%= link_to 'Korisnici', users_path %></li>
        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Akcije <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Nova Objava', new_article_path %></li>
            </ul>
          </li>
        <% end %>
        
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Kategorije <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Sve kategorije', categories_path %></li>
              <% Category.all.each do |cat| %>
                <li><%= link_to "#{cat.name}", category_path(cat) %></li>
              <% end %>
              <% if logged_in? and current_user.admin? %>
                <li role="separator" class="divider"></li>
                <li><%= link_to "Kreiraj novu kategoriju", new_category_path %></li>
              <% end %>
            </ul>
          </li>
        
      </ul>
      <form class="navbar-form navbar-left">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Pretraga">
        </div>
        <button type="submit" class="btn btn-default">Potvrdi</button>
      </form>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li><%= link_to 'Izloguj se', logout_path, method: :delete %></li>
          
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" 
            aria-expanded="false">Tvoj nalog <%= "Admin" if current_user.admin? %><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Izmeni svoj profil", edit_user_path(current_user) %></li>
              <li><%= link_to "Pregledaj profil", user_path(current_user) %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'Uloguj se', login_path %></li>
          <li><%= link_to 'Registruj se', signup_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>